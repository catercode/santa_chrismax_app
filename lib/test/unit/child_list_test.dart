// import 'package:flutter_test/flutter_test.dart';

// void main() {
//   group('ChildList', () {
//     test('should add a child to the list', () {
//       final childList = ChildList();
//       final child = Child(name: 'John', country: 'USA');

//       childList.addChild(child);

//       expect(childList.state, [child]);
//     });

//     test('should toggle the nice status of a child', () {
//       final childList = ChildList();
//       final child = Child(name: 'Alice', country: 'Canada');

//       childList.addChild(child);
//       childList.toggleNiceStatus(0);

//       expect(childList.state[0].isNice, false);
//     });
//   });
// }
